<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
  "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
  "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
    Checkstyle ICDE848 TP Jenkins - VERSION CORRIGÉE
    Compatible Jenkins + Maven Checkstyle Plugin

    IMPORTANT :
    - LineLength DOIT être hors TreeWalker
-->

<module name="Checker">

    <!-- Configuration globale -->
    <property name="severity" value="warning"/>
    <property name="fileExtensions" value="java"/>

    <!-- ─────────────────────────────────────
         LONGUEUR DE LIGNE (HORS TreeWalker)
    ───────────────────────────────────── -->
    <module name="LineLength">
        <property name="max" value="120"/>
    </module>

    <!-- ─────────────────────────────────────
         ANALYSE DU CODE JAVA
    ───────────────────────────────────── -->
    <module name="TreeWalker">

        <!-- ───────── NOMMAGE ───────── -->

        <module name="TypeName"/>
        <module name="MethodName"/>
        <module name="LocalVariableName"/>
        <module name="ConstantName"/>

        <!-- ───────── COMPLEXITÉ / TAILLE ───────── -->

        <module name="MethodLength">
            <property name="max" value="50"/>
        </module>

        <module name="ParameterNumber">
            <property name="max" value="7"/>
        </module>

        <!-- ───────── QUALITÉ CODE ───────── -->

        <module name="EmptyCatchBlock"/>
        <module name="UnusedImports"/>
        <module name="AvoidStarImport"/>
        <module name="EqualsHashCode"/>
        <module name="ParameterAssignment"/>

        <!-- ───────── STYLE JAVA ───────── -->

        <module name="NeedBraces"/>
        <module name="LeftCurly"/>
        <module name="RightCurly"/>

    </module>

</module>

<!--
═══════════════════════════════════════════════════════
RAPPEL DES RÈGLES
═══════════════════════════════════════════════════════

✔ LineLength → hors TreeWalker
✔ MethodLength → dans TreeWalker
✔ Naming rules → dans TreeWalker

❌ Erreur que tu avais :
TreeWalker is not allowed as parent of LineLength

✔ Maintenant corrigé
-->